# Delete-first replacement recovery

Independent hypothesis, 2026-09-12 (before prior-art discussion search): a confirmed-deleted replacement keeps its three-send lifetime budget while topic binding deliberately retains its anchor. Every subsequent task joins that exhausted anchor. The ambiguous-send fence is correct; applying an exhausted lifetime budget to definitely rejected sends permanently disables a topic.

Contract: retain delete-first ownership and the complete row projection. After a bounded three-attempt burst of definite rejections, only newly admitted delegation work may grant another bounded burst. Tool events, duplicate starts, reconciliation, and restart alone do not replenish it. Preserve the last server cooldown across exhaustion and restart. A start arriving during transport must not be lost, but an ambiguous send remains fenced regardless of later work. No live ledger repair, runtime promotion, restart, or outcome disposition is part of this patch.

Test boundary: real DelegationCards observe/reconcile, JSON persistence, TelegramAdapter and shared scheduler, fake Bot API only. Verify exhausted recovery, repeat exhaustion, new work during the last send, restart, terminal preservation, duplicate-start idempotency, and ambiguous acceptance without duplicate cards.

Prior art: current fork main remains `a8998693befd1190ab462a5c18494a0842ef5e51`; anchor history identifies fork PR #152 as delete-first implementation. Fork open/all-state reanchor search found no recovery fix. Upstream search skipped for this fork-owned overlay.

Integration (2026-09-12): stacked on the exact published PR #166 head `8295f053e0d19e94984f271db07b798021aa6535`. GitHub readback reports OPEN with no merge commit; remote main remains `a8998693befd1190ab462a5c18494a0842ef5e51`. The published dependency contains the original disposition commit `f6c88cbd3b8`, regression-double correction `a8d22213e5d`, and locked-test-dependency correction `8295f053e0d`; all are preserved as the base, not rewritten into this patch. The prior candidate is preserved in stash object `ce8aa1f2e1470a8e10184649db0748c3796de8c3`. Only the already-validated same-row `subagent.admitted` early-return path additionally marks pending new work. Rejected, unclaimed, and duplicate admissions do not grant recovery. If #166 squash-lands later, the parent must verify byte-identical dependency tree mapping before changing the review base. The dependency is not landed or active; the other owner's worktree remains untouched.

Verification (2026-09-12): regression on unchanged base failed in four definite-failure/restart cases (`3 == 3 + 3`); last-send interleaving regression also failed (`3 == 4`), while ambiguous controls passed. Final candidate: `scripts/run_tests.sh tests/gateway/test_delegation_card_anchor.py tests/gateway/test_delegation_cards.py tests/gateway/test_delegation_card_consolidation.py tests/gateway/test_delegation_card_reconciliation.py tests/gateway/test_delegation_handling.py` passed 104 tests, no failures/retries. `uv tool run ruff check .`, `python3 scripts/check-windows-footguns.py --all`, and `git diff --check` passed (Ruff reported three unrelated existing invalid-noqa warnings). Full repository suite and hosted matrix were not run. Parent-owned independent review and guarded integration remain outstanding; no source commit/publication or runtime mutation performed by implementer.

Combined-candidate verification (2026-09-12): before the four-line admission integration, `scripts/run_tests.sh tests/gateway/test_delegation_card_anchor.py -k 'exhausted and same_row'` reproduced four failures (`3 == 3 + 3`) and passed both ambiguous-send controls. After integration, the canonical runner passed 147 tests across the original five gateway modules plus `tests/gateway/test_delegation_dispositions.py`, `tests/gateway/test_delegation_result_retention.py`, `tests/gateway/test_delegation_unit_delivery.py`, and `tests/tools/test_delegation_same_row_resume.py`, with zero failures/retries. Both existing regression functions now cover new-start and same-row work, including admission rejection/replay, repeated exhaustion, restart, cooldown, in-flight admission, terminal-only suppression, and ambiguous sends. Existing rate-budget modules `tests/test_telegram_per_chat_send_budget.py` and `tests/test_telegram_typing_chat_budget.py` passed another 29 tests. Repository-wide Ruff, Windows-footgun scan (1585 files), and diff whitespace checks passed. No full-suite or hosted-matrix claim; review dispatch, commit, publication, merge and activation remain parent-gated.

Rate boundary: replacement still uses the existing expendable `send_delegation_card` path and shared per-chat gate; no new transport path or limiter is introduced. Existing default budgets allow `60/2 + 60/4 = 45` calls/minute for DM sends plus typing, and `60/6 + 60/12 = 15` for groups (75% of the respective community envelopes). Edits/deletes share the send budget. This is not a guarantee for arbitrary overrides: existing configurable zero gaps and non-card forum-topic/fallback behavior are unchanged. Additional accepted work extends exposure only through bounded bursts, preserving durable server cooldown and final-reply priority; ambiguous sends never retry.

Origin: fork-owned deletion-first replacement overlay. Retirement: replace completely if upstream provides the same bounded, certainty-aware recovery contract. Rollback: revert this candidate's source/test changes; added receipt fields are optional and ignored by old code (rollback restores the original exhaustion limitation).
