# Repository CI contract

`./bin/ci preflight` is fast, credentialless local feedback; the optional `.githooks/pre-push` hook invokes it (set `HERMES_SKIP_PREFLIGHT=1` to bypass). Existing global `core.hooksPath` takes precedence: this repository does not install or override hooks. `./bin/ci gate <40-character-head-sha>` is the blocking Linux PR profile: static policies, four focused agent/gateway/CLI Python files, and six bounded Node workspace checks (the complete nine nonrelease workspace checks run nightly). The complete non-integration Python suite runs nightly, not per PR. `./bin/ci nightly <sha>` runs all seven source lanes on Linux, including full Python, E2E, docs, Rust, and container lint. `./bin/ci nightly-native <sha>` selects the pre-existing actual-host marked native suite, but is not an automated qualification here. Both exact-SHA profiles reject a wrong SHA or dirty tracked tree before setup, immediately before checks, and after checks (including failed checks). The existing `setup`, `check`, `full`, `list` and partial `check --lane` interfaces remain available.

The fork's `main` has **no workflow files** at this change's base (`9abae85c03`). Its inherited CI workflows were retired earlier; [portable-ci.md](portable-ci.md#coverage-allocation-before-workflow-removal) maps all 36 original files and checks. This PR adds only `gate.yml` and `nightly.yml`; it does **not** delete a live workflow. Both use a pinned Node Linux image, SHA-pinned checkout, exact ref assertion and read-only token permissions. The PR's `qualification` job is the sole aggregation status; do not require a cancelled/skipped lane as success. Nightly's concurrency never cancels an earlier night. Existing upstream-sync automation is external to this repo's current workflows and is not modified.

| Previously active workflow at last CI-enabled revision | Disposition |
| --- | --- |
| `ci.yaml` and reusable `tests.yml`, `lint.yml`, `js-tests.yml`, `rust-tests.yml`, `docs-site-checks.yml`, `docker-lint.yml` | Linux gate takes static/focused Python/six bounded Node checks; nightly adds complete Python/nine Node/E2E/docs/Rust/container lint. See `portable-ci.md` for every subcheck. |
| `history-check.yml`, `contributor-check.yml`, `uv-lockfile-check.yml`, `infographic-check.yml`, `profile-artifact-check.yml`, `case-collision-check.yml`, `fork-patch-check.yml`, `plugin-catalog-ci.yml` | Static gate, via the existing source checks. |
| `tests-os.yml`, `installer-tests.yml`, `windows-venv-e2e.yml` | Native OS and installer release evidence remain manual/on-demand in `portable-ci.md`; Linux cannot qualify them. No Windows/macOS default PR jobs. Human decision needed before reinstating native hosted nightly. |
| `nix.yml` | Advisory `nix flake check` remains manual; not a source correctness gate. |
| `docker.yml` | Upstream-owner release publish/build stays retired; container-lint nightly is not image execution. `tests/docker` requires separate container-capable qualification for relevant changes. |
| `install-e2e.yml`, `install-e2e-run.yml`, `install-e2e-macos-run.yml`, `install-e2e-windows-run.yml` | Scheduled/release installer integration remains a separate release decision; not replaced by source green. |
| `skills-index.yml`, `skills-index-freshness.yml`, `deploy-site.yml` | Upstream-owner publication and live freshness remain retired; nightly docs build is not deployment. |
| `ci-review-comment.yml`, `publish-e2e-evidence.yml` | Trusted presentation/evidence workflows remain retired, not part of gate. No review-comment polling runner is reintroduced. |
| `lockfile-diff.yml`, `supply-chain-audit.yml`, `review-labels.yml`, `label-rerun.yml`, `osv-scanner.yml`, `js-autofix.yml`, `e2e-desktop.yml` | Dependency bounds and locked setup in static gate; dependency-diff and vulnerability scans remain advisory; custom label/autofix and already-disabled desktop E2E remain retired. |

Cost estimate (billed Linux runner minutes; workload dependent): before retirement the last 30 job-bearing CI runs averaged ~60 min, Nix ~5.6/run, Docker ~1/run, plus review-comment ~4/run across 100 triggers; roughly **70 min/PR**, excluding duplicate push runs and Windows/macOS matrices. The replacement is one Linux focused source gate estimated **8–18 min/PR plus ~1 min qualification**; nightly broad Linux profile roughly **55–70 min/night plus ~1 min qualification**. No hosted native platform charges. The former `CI review comment` workflow started on every `CI` **in_progress** and polled up to 50 minutes; among the last 100 runs its 60 active jobs billed roughly 401 Linux minutes, 61 of them cancelled. Do not revive it. If comments become a requirement, use a completed-event single commenter with no polling and restrict its write privilege to a trusted workflow, not PR-controlled gate code.

A passing source gate does not certify external integrations, Nix, native installers, image publication, or signed desktop releases. GitHub Actions billing limits may prevent all jobs from starting; until an actual hosted run executes, local verification is evidence only and `qualification` cannot be claimed green.
