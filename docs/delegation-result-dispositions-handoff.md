# Disposition + same-session resume: preserved delivery handoff

## Current status and next gate

The preserved candidate remains in `/Users/brianle/Repos/.worktrees/hermes-result-dispositions`, branch `fix/delegation-result-dispositions`, base `a8998693befd1190ab462a5c18494a0842ef5e51`. No source landing, runtime activation, DB maintenance or production-card mutation has been performed by the revision owner. Existing staged source was preserved; the four findings from `deleg_68882309` and the two subsequent retention findings from `deleg_1557a774` were addressed in place. The candidate base was fast-forwarded to the landed `review_changes` rename, then the exact saved working changes reapplied; no active old-name entrypoint was resurrected. Recovery stash `f39267353f63f28c1893203fed9b9c53deb1fc8d` and `pre-rename-reconciliation.patch` remain available. The [current contract](delegation-result-dispositions.md) is authoritative; the historical receipts below are not current policy or approval.

Native review `deleg_c7407695` evaluated `sha256:82ffb35a2bb104d55eae93b8ee15152d5ed97e6376401876525cb8b621d09e7e`, actual `custom:local-proxy/gpt-6-astra`, and returned **REQUEST_CHANGES**. This was the user's explicitly authorized same-provider/non-independent availability fallback. Do not retry obsolete Claude CLI quota probes. Parent must dispatch `review_changes` on the revised exact accepted paths; the executor forbids child dispatch. Do not land before its findings are resolved and approval is recorded. User already authorizes guarded fork landing after approval; no extra user approval is needed for that source operation.

Canonical checkout is `/Users/brianle/.hermes/hermes-agent`. `/Users/brianle/Repos/hermes-agent` is an older separate checkout with unrelated MAINTENANCE work and was not touched. Preserve current main and the concurrent review-guidance owner's changes. The revised review-argument and test-evidence artifacts live under `/Users/brianle/Repos/.artifacts/hermes-result-dispositions/`.

## Revision outcomes

1. Hidden correction isolates/restores all prose callbacks, including streaming/TTS/interim/tool channels. Only exact ledger control events pass. Model correction output and tool messages are hidden; original answer remains the delivered answer.
2. `agent/delegation_correction.py` runs an explicit maximum of two native requests, not a normal loop whose finalizer could add summary/retry calls. It avoids compression/title/memory/summary/stop auxiliaries and fan-out fallback. Unsupported unbounded transports retain the warning instead of repair. HTTP SDK integration tests exercise actual request counts, errors, two tool rounds and restoration.
3. Async completion metadata derives exact thread refs and attempts from actual unit entries. Early failure notices carry exact failed-unit identity. Coalescing preserves attempts without mutating queued originals; unrelated terminal sibling results are not presented/gated.
4. Process accounting precedes the resume checkpoint. Native owned running/unreconciled process registry blocks grants, including silent jobs; blockers persist before advertising a result. Real harmless subprocess + reopened SQLite tests cover running/unread/consumed/foreign boundaries.
5. `delegate_task(action="result", delegation_id=...)` retrieves an exact immutable owner's durable terminal unit. Normal result insertion registers the actual refs/attempts in a later ordinary turn. Reading does not acknowledge; a prior deferral remains until deliberate handling. No global historical scan/gate was added.

The new regression files are `tests/agent/test_delegation_correction_real_loop.py`, `tests/gateway/test_delegation_unit_delivery.py`, and `tests/tools/test_delegation_process_checkpoint.py`. Final canonical revised run: **59 files, 786 passed, 0 failed, 15 platform skips**, 48.0 seconds (12 workers). The three new files contribute 13 passing integration/boundary cases. `git diff --check HEAD`, offline Ruff on the new correction/disposition and regression modules, and the repository compatibility-pointer check pass. Exact evidence and review arguments are recorded in `revision-test-evidence.json` and `revised-native-review-controller-args.json`; historical counts below are not reused as final-snapshot proof. An earlier run used `--log-level=ERROR`, which suppressed a pre-existing warning-capture assertion; its log is retained as `revision-focused-tests-loglevel-error.log`. The full corrected runner invocation, with default logging, passed without changing that test or its production code.

## Queued user-authorized historical reconciliation

After the lifecycle fix is reviewed/landed, parent owns one-time cleanup/reconciliation of terminal delegation-card states across **all default-profile sessions/topics**, including the Persistent VPS Agent Setup screenshot at `~/.hermes/cache/images/img_30019ebf2b70.jpg`. Parent is separately collecting a read-only inventory. Do not mutate those cards while this candidate is under review.

Preserve active work and unfinished obligations. Resolve only exact owner/ref/attempts with supported disposition/delivery receipts; failure/interruption may be honestly reported, never relabeled execution success. No age-based auto-accept, raw deletion, speculative identity repair or worker resume. If a legacy row lacks provable identity or the supported lifecycle cannot express its safe reconciliation, report the precise missing capability for a narrow follow-up instead of bypassing it. Runtime upgrade/restart remains excluded: coordinate activation with parent if needed before executing cleanup. Source landing alone does not activate behavior.

## Prior boundary revision evidence (superseded exact candidate)

Four fixes: atomic owned-process snapshot spans exit publication; repair admits only zero-retry native SDK clients and now supports retry-free Codex SSE; all inline terminal results receive persisted exact-owner result identities; direct forced host printing is suppressed/restored. Focused tests exercise paused real readers, actual HTTP request counts for chat/Codex success and errors, ACP rejection before launch, interrupted handle output, and synchronous/mixed fallback defer → later retrieve → handle.

Fresh final-snapshot tests and exact accepted-path manifest are in `boundary-test-evidence.json` / `boundary-candidate-manifest.json`; parent controller arguments are `boundary-review-changes-controller-args.json` in the artifact directory. Earlier counts below describe only earlier candidates. Parent dispatches native `review_changes`, reads its exact-candidate verdict, resolves actionable findings, then performs the already-authorized guarded landing. This revision owner has not invoked review or landed source.

Activation/cleanup sequence for parent: after source approval/landing, record the landed commit and inspect the canonical installed/runtime revision and active-work inventory. Use the supported runtime update/drain workflow only under the parent's activation authorization, never kill this work or restart blindly. Verify the running revision and a fresh result-disposition smoke receipt before executing the separately authorized exact historical terminal-card reconciliation. Inventory all default-profile sessions/topics read-only first; preserve raw rows, unresolved obligations, frozen identity and delivery receipts. If the supported lifecycle cannot represent a historical row safely, return that exact blocker rather than mutate storage directly.

## Prior retention revision (superseded candidate)

Native `deleg_1557a774` requested changes on exact candidate `sha256:5de373e713728d39d537d4c496d84f9797142b8b1c67e2454df1c739da1d7c27`. This revision fixes both defects using existing storage/lifecycle machinery, not a new service or retention manager:

- Unobserved finished process effects retain an exact raw-owner/process fence in the existing redacted, output-size-bounded completed receipts. Memory TTL/cap pruning may evict the payload only after persistence; receipt history pruning excludes unresolved obligations. Poll/log/wait/output-consuming reconciliation persist observation. Reopened registries and SQLite checkpoints remain blocked until exact observation; settled receipts still expire. Persistence errors do not silently erase the in-memory fence.
- Existing result-ledger rows default-retain named exact attempts from dispatch metadata plus actual unit payloads, before any presentation callback and through notification acknowledgement/drop/deferral. Exact card delivery or proven admitted revision/replacement writes release evidence into that same row. Normal history pruning resumes after release, including inline archives. Notification retries are untouched, and all live memory states remain non-evictable.

Fresh canonical tests, source hashes and full accepted scope are in `retention-test-evidence.json`, `retention-candidate-manifest.json`, and `retention-review-changes-controller-args.json` under the artifact directory. Prior 924-pass/66-file evidence belongs to the prior candidate, not this revision. Parent alone dispatches the native `review_changes` gate over **all** accepted paths, records its exact candidate ID and verdict, then performs already-authorized guarded landing after approval. Configured Astra remains user-authorized same-provider/non-independent review. No child review dispatch, source landing, live configuration/DB mutation, runtime activation, or historical-card reconciliation was performed here; activation/history work remains separately queued.

## Prior three-gap revision (superseded exact candidate)

Native `deleg_69a5b6de` returned REQUEST_CHANGES for exact candidate `sha256:24db054a7c8e6653365a6462c43ad7c8c7eddf6f536fa876706cfd6ee75f0f8e`, actual Astra. The revision preserves the accepted source and resolves the three concrete persistence/lifecycle gaps:

- Base-format receipts without literal true observation proof stay unresolved. Age/cap pruning and reopened SQLite claims remain blocked until exact observation. Corrupt receipts fail closed without erasure; failed history pruning no longer falsely reports a successful receipt publication as failed.
- Failed completed-receipt publication writes a force-redacted bounded completed snapshot into the existing `processes.json` checkpoint. Later successful checkpoint writes preserve it. Recovery restores only read-only completion state and exact ownership, not live PIDs, watchers, deadlines, or notifications. Failed observation writes remain conservative. A newer exact receipt supersedes a stale fallback checkpoint after cleanup-write failure.
- Inline archive and exact release lifecycle boundaries invoke existing settled-history pruning. Tests repeatedly exercise production archive/release APIs without a private-pruner call, retaining unresolved results while bounding released history.

The observation/recovery matrix covers missing/null/mistyped proof, corrupt/truncated/non-object/mismatched receipts, exit/read transitions, receipt-write failure plus successful checkpoint persistence, repeated registry recovery, and stale checkpoint cleanup after successful observation. This is not a guarantee when both storage writes fail or a user deletes retained storage; no new service or retention manager was introduced.

The new artifacts are `review-gaps-test-evidence.json`, `review-gaps-candidate-manifest.json`, and `review-gaps-review-changes-controller-args.json` in the existing artifact directory. `review-gaps-red.log` preserves the reproductions. Canonical run: **74 files, 1067 passed, 0 failed, 15 platform skips**, 69.7 seconds (12 workers). Final counts and hashes in the new evidence describe this exact revision; historical 938-pass evidence remains intact and is not reused as current verification. `tools/process_registry_checkpoint.py` is newly included in accepted review scope. Parent must run `review_changes` over **all** accepted paths, record the new exact candidate verdict, then perform guarded authorized landing only after approval. No review dispatch, commit/landing, runtime activation, historical cleanup, configuration or business-DB writes were performed by this revision owner.

## Current final lifecycle revision and parent-only gate

Native `deleg_0d3198f6` returned REQUEST_CHANGES for `sha256:7466a3724a095652a2f29039ed478161ee855ebaaf459f443ff984adc529bc07`. The previous three findings were closed; this revision addresses its concrete P2-A, P2-B and P3 without a new storage service:

- **P2-A:** Ordinary conversation ownership is proven read-only from the existing session DB, including verified compression ancestry, so normal gateway receipts use the existing age/count budget without forging observation. Child, ended-child and unknown ownership remain retained until exact reconciliation. All directory parsing/pruning is outside the registry lock. A generation check covers both exit publication and failed-publication eviction before the atomic final live/retained snapshot.
- **P2-B:** Malformed files remain in place but do not erase healthy history. Intact exact identity/raw-owner metadata or consistent checkpoint identity isolates the affected owner. Truncated/non-object/mismatched data with irrecoverable or conflicting owner metadata conservatively blocks all owner-based resume checks, not user-facing healthy retrieval. No guessed owner or automatic corruption repair.
- **P3:** Per-entry recovery preserves malformed originals across future checkpoint rewrites, keeps their uncertainty scoped where provable, recovers later healthy live entries/watchers, and never adopts/replays a completed fallback.

The added real-storage regression matrix first reproduced **7 failures / 2 passes** on the prior source (`final-lifecycle-red.log`). The complete previous 74-file canonical suite was rerun: **1078 passed, 0 failed, 15 platform skips**, 223.4 seconds (12 workers), `final-lifecycle-full.log`. Existing correction/Codex/ACP/output, per-unit delivery, same-session/frozen-authority and result-retention paths were preserved. The final accepted-path hash manifest, candidate-bound affected-suite evidence and exact schema-valid parent tool arguments are `final-lifecycle-candidate-manifest.json`, `final-lifecycle-test-evidence.json`, and `final-lifecycle-review-changes-args.json` in the existing artifact directory.

Tradeoffs remain explicit: unresolved resumable children are deliberately not age-expired; unknown ownership may require evidence repair before any resume can be proven safe. Missing/unavailable session metadata is conservative rather than a guessed ordinary owner. A history scan is still linear in retained obligations, but never under the registry lock. Exact single-file publication and checkpoint snapshot/write retain their existing lock boundary. Both durable writes failing, or external deletion, remains outside recovery guarantees.

Parent must run `review_changes` on all 39 accepted paths and record its new exact candidate verdict before already-authorized guarded landing. This revision owner performed no review dispatch, landing, runtime activation, historical cleanup, configuration or business-data writes. Preserve unrelated source/main and queued historical reconciliation boundaries.

## Historical receipts (superseded policy; retained evidence only)

- Earlier canonical focused run: 46 files / 613 passes; later warning/same-session rerun: 10 passes. Retry-focused run: 47 files / 633 passes. These predate the five native fixes.
- Broad phased evidence: 1,630 per-file results / 18,285 passes, no remaining scoped failed/unrepresented files in the archived manifest. It is not one uninterrupted final-snapshot suite.
- Original Claude-only AutoReview run `fe4f9f4225f844939bb8e540dd52b267`, candidate `2b31ac9a6894d045b908f333fee2034e09ae71fc52f7e88bd6d86e27a96f52d5`; retry run `e11a8e848446470d92c1ab14e6fb3986`, candidate `1385c29798e8d67ce1539cefe18ed712e67beed8a227fc08611455e908ba4415`. Fable and Opus both returned HTTP 429 monthly spend limit, zero inference tokens; exit 3/blocked was not review approval. Captured evidence is retained, not synthesized. The user later authorized native Astra fallback, which completed with substantive findings.
- Prior original base `147b1519b9379a7b9d1ea784836c96ffbc685bdb` was reconciled to fork PR #163 (`cfad11979f9`); recovery stash `59af3d4969241d142985a9ee7d8129dc19c572da` preserves the earlier candidate.
- Artifact directory retains `test-evidence.json`, `retry-focused-tests.log`, `hermes-result-dispositions-final-tests.log`, `final-boundary-regressions.log`, broad/continuation logs, failed review logs and sanitized `resume-investigation.md`.
- Historical interrupted/Recover screenshot rows lacked durable child identity/linkage. They were inspected read-only, not accepted/deleted/resumed. Resolver/authority drift remained fail-closed. These observations do not authorize rewriting old frozen authorities.

Cards/result disposition remain fork-specific in MAINTENANCE; no new upstream issue or unrelated upstream contribution was changed.
